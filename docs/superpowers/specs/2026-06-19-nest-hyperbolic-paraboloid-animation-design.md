# 鸟巢双曲抛物面动画 — 设计文档 (Spec)

- **日期**: 2026-06-19
- **状态**: Implemented（已通过第三阶段开发实施完成）
- **关联实施计划**: `docs/superpowers/plans/2026-06-19-nest-hp-animation-plan.md`
- **关联源代码**: `src/matlab/`（`nest_geometry.m`, `nest_animation.m`, `run_nest_demo.m`）
- **关联测试**: `src/matlab/test/test_nest_geometry.m`（7/7 通过）
- **关联产出**: `out/nest.mp4`, `out/nest.gif`
- **主题**: MATLAB 多阶段动画，课堂演示「用直线搭建双曲抛物面（马鞍面）」
- **关联设计稿**: `designs/nest-hp-animation/Nest Animation Design.html`
- **可视化预览**: `http://localhost:4311/nest-hp-animation/Nest Animation Design.html`

---

## 1. 背景与目的

课堂需要直观演示一个核心数学事实：**双曲抛物面（马鞍面）是双重直纹面** —— 它可以完全由直线构成，且过曲面上每一点恰好有两条直线落在曲面上。

以「鸟巢」（国家体育场）的真实结构为载体：外围一圈立柱支撑一个马鞍形屋顶，而屋顶的钢骨架就是双曲抛物面的两组直纹母线。通过动画分阶段「建起」这个结构，让学生亲眼看到：

1. 两组母线**确实是直线**（一条母线只需两个端点定义，中间不采样即落在曲面上）；
2. 这两组直线交织，**勾勒出整个马鞍面**；
3. 立柱顶点**贴合**曲面的高低起伏。

## 2. 目标与非目标

### 目标
- 交付一段可在课堂 MATLAB 中实时运行的多阶段动画。
- 同时提供导出 `mp4` 和 `gif` 的视频/动画版本，脱离 MATLAB 可播放。
- 动画清晰呈现三阶段：空坐标系 → 蓝立柱 → 橙母线屋顶。
- 阶段间用「逐根出现」过渡，节奏适合讲解。
- 视角为侧面俯视（az=−35°, el=30°），马鞍两向翘起都可见。
- 代码模块化，几何部分可独立单元测试（验证母线是直线）。

### 非目标 (YAGNI)
- **不**追求真实鸟巢的复杂次结构/编织纹理/材料渲染（仅取其「椭圆环立柱 + 马鞍屋顶」的几何骨架）。
- **不**做交互式 GUI / App Designer（纯脚本驱动即可）。
- **不**支持多语言字幕、配音、配乐。
- **不**支持实时参数滑块界面（参数集中在脚本顶部常量，改完重跑）。

## 3. 已确认决策（来自 brainstorming）

| 维度 | 决定 | 备注 |
|------|------|------|
| 两组不同颜色构件的含义 | 立柱(颜色①) + 屋顶直纹母线(颜色②) | 两步渲染对应两组构件 |
| 交付形式 | 实时运行脚本 + 导出 mp4/gif | `run_nest_demo(record=true)` |
| 构件密度 | 稀疏清晰 | 立柱 N=10，每组母线 ≈9 |
| 立柱平面布置 | **椭圆环**（真实鸟巢外形，不简化为矩形/圆形） | 长短轴 a=6, b=5（比例 1.2，贴近真实鸟巢约 1.21） |
| 柱顶与屋盖关系 | **变高贴合曲面** | 每柱顶 z = 曲面在该(x,y)处 z 值 |
| 过渡动画类型 | **生长/延展动画** | 立柱从地面逐帧升起（~8帧/根），母线从一端逐帧延展（~10帧/条） |
| 构件出现顺序 | 立柱逆时针逐根；母线先 u 组后 v 组，每组内沿参数扫掠 | 立柱从 θ=0（长轴端）起逆时针 |
| 母线延展方向 | 同向延展（参数负→正） | u 组每条从 v=−1→+1，v 组每条从 u=−1→+1 |
| 相机视角 | 标准侧俯 az=−35°, el=30°（全程固定） | MATLAB `view([-35,30])` |
| 代码架构 | **模块化**（几何+动画+驱动+测试） | 见 §6 |
| 直纹母线组数 | **两组都画**（u=const 与 v=const） | 双重直纹面要点 |
| 母线配色 | **深浅区分**：u 组 #EDB120（标准橙），v 组 #FFD060（浅橙） | 互补、色盲友好 |
| 曲面背景 | 母线下叠**半透明曲面片** (alpha≈0.2)，阶段 2 开始时先曲面再母线 | 增强线条与曲面的视觉关联 |
| 录制策略 | **主动帧循环录制** | 每间帧 writeVideo 多帧，不依赖 pause 实际时长 |
| gif 导出 | **需要** | mp4 + gif 双格式交付 |
| 地面渲染 | 仅椭圆环（不画方格网） | 保持简洁 |
| 阶段标题 | 中文标注：阶段 1「立柱升起」、阶段 2「直线构成曲面」 | 随阶段切换 |

## 4. 核心数学

### 4.1 曲面方程

标准双曲抛物面（马鞍面）：

```
z = c · (x²/a² − y²/b²)
```

其中 `a, b` 取椭圆长短半轴，`c` 为翘起缩放量（控制马鞍起伏明显度，默认 2.5）。

### 4.2 因式分解 → 双重直纹证明

```
z = c · (x/a − y/b)(x/a + y/b)
```

引入参数 `u, v`：

```
u = (x/a − y/b) / 2
v = (x/a + y/b) / 2
```

则 `x = a(u + v)`, `y = b(v − u)`, 代入得 `x²/a² − y²/b² = (u+v)² − (v−u)² = 4uv`，因此 `z = c·(x²/a² − y²/b²) = 4c·u·v`。

> 实现时统一采用 §4.3 的 `/2` 标度（`x=a(u+v)/2`），则 `x²/a² − y²/b² = uv`，`z = c·u·v`。两者只差一个 u,v 的标度常数（§4.2 的 u 是 §4.3 的 2 倍），曲面形状完全相同。下文一律以 §4.3 标度为准。

**证明**：固定 `u = u₀`，则 `z = c·u₀·v` 随 `v` **线性**变化 → 落在曲面上的是一条**直线**。同理固定 `v = v₀` 得另一条直线。因此过曲面上每点 `(u₀, v₀)` 恰有两条母线，曲面是双重直纹面。

### 4.3 参数化（实现用）

令 `u, v ∈ [−1, 1]`（规范化域），映射：

```
x = a · (u + v) / 2
y = b · (v − u) / 2
z = c · u · v
```

> 校验：当 `u=v=1` → x=a, y=0, z=c；当 `u=1,v=−1` → x=0, y=−b, z=−c。符合马鞍「长轴两端上翘、短轴两端下垂」的几何。

### 4.4 母线的「两端点」定义（关键）

每条母线只需取两个端点，`plot3` 一笔画直线，**无需中间采样**：

- **u = const 组**：取 `v₁ = −1` 与 `v₂ = 1`：
  - `P₁ = (x(u,v₁), y(u,v₁), z(u,v₁))`
  - `P₂ = (x(u,v₂), y(u,v₂), z(u,v₂))`
- **v = const 组**：取 `u₁ = −1` 与 `u₂ = 1`：
  - `P₁ = (x(u₁,v), y(u₁,v), z(u₁,v))`
  - `P₂ = (x(u₂,v), y(u₂,v), z(u₂,v))`

母线条数取 `k = 1 … M−1` 处的 `u = −1 + 2k/M`（跳过边界 `u=±1`，因其重合于椭圆边界，避免冗余）。默认 `M_u = M_v = 9` → 每组 8 条内母线。

### 4.5 立柱位置与高度

立柱沿椭圆环均匀分布：

```
θ_i = 2π · i / N,   i = 0 … N−1
x_i = a · cos(θ_i)
y_i = b · sin(θ_i)
柱顶 z_i = c · (x_i²/a² − y_i²/b²)
```

> 注：`(cos θ, sin θ)` 使 `(x/a)²+(y/b)²=1`，故立柱恰在椭圆边界上。柱顶 `z_i = c·(cos²θ_i − sin²θ_i) = c·cos(2θ_i)`，自然产生长短轴方向的高低差（长轴端 z=+c 高翘、短轴端 z=−c 下沉），呈现马鞍。

## 5. 几何参数（默认值，集中常量）

| 参数 | 符号 | 默认 | 说明 |
|------|------|------|------|
| 椭圆长半轴 | `a` | 6 | 鸟巢长轴半径 |
| 椭圆短半轴 | `b` | 5 | 鸟巢短轴半径（比例 1.2，贴近真实） |
| 立柱数量 | `N` | 10 | 椭圆环均匀分布 |
| 翘起量 | `c` | 2.5 | z 缩放，控制马鞍起伏明显度 |
| u 组母线条数 | `M_u` | 9 | 跳边界后实际 8 条内母线 |
| v 组母线条数 | `M_v` | 9 | 同上 |
| 方位角 | `az` | −35 | `view([az,el])`，全程固定 |
| 仰角 | `el` | 30 | `view([az,el])`，全程固定 |
| 立柱生长帧数 | `n_pillar` | 8 | 每根立柱升起过程的中间帧数 |
| 母线延展帧数 | `n_rule` | 10 | 每条母线延展过程的中间帧数 |
| 逐根间隔 | `dt` | 0.15 s | `pause(dt)`，每根构件生长完成后到下根的间隔 |
| 阶段间停顿 | `hold_t` | 0.8 s | 阶段完成后整体停顿供讲解 |
| 录制帧率 | `fps` | 30 | VideoWriter |
| 曲面 alpha | `alpha_surf` | 0.2 | 半透明曲面片 |

## 6. 代码架构（模块化）

```
src/matlab/
├── nest_geometry.m       % 纯几何：返回立柱坐标、母线端点、曲面网格
├── nest_animation.m      % 3 阶段渲染 + 逐根过渡（操作 figure/axes/plot3）
├── run_nest_demo.m       % 驱动入口 + record 开关 → VideoWriter 导出
└── test/
    └── test_nest_geometry.m  % 单元测试（验证母线是直线）
```

### 6.1 `nest_geometry.m`

纯函数，无副作用，可独立测试。

**输入**：参数结构体 `p`（含 a, b, c, N, M_u, M_v）。

**输出**：结构体 `G`：
- `G.pillars`：`[3×N]` 矩阵，每列一根立柱的 (x, y, z_top)。
- `G.rule_u`：cell `{1×(M_u−1)}`，每个 cell 是 `[3×2]` 的两端点（u=const 母线）。
- `G.rule_v`：cell `{1×(M_v−1)}`，同上（v=const 母线）。
- `G.surf_mesh`：`[X, Y, Z]` 网格（供 `surf` 半透明片）。
- `G.ellipse_pts`：`[2×128]` 椭圆边界点（供画地面环）。

### 6.2 `nest_animation.m`

**输入**：`G`（几何）、`p`（参数）、`ax`（axes 句柄，可选）、`recorder`（VideoWriter 句柄，可选）。

**行为**：分三阶段渲染，阶段间生长/延展动画：

**阶段 0 · 空坐标系**
- `hold on`，画椭圆地面环（灰色 `#7F8C8D`）+ 三轴标注（X/Y/Z）
- `view([az,el])`, `axis equal`, `pause(hold_t)`
- 坐标轴范围：`xlim([-a-1, a+1])`, `ylim([-b-1, b+1])`, `zlim([-2c, 2c])`

**阶段 1 · 蓝立柱升起**
- `title('阶段1：立柱升起')`
- 立柱按逆时针顺序（θ=0 起），每根执行**生长动画**：
  ```
  for frame = 1:n_pillar        % 默认 8 帧
      t = frame / n_pillar        % 生长进度 [0→1]
      z_current = t * z_i
      更新 plot3 线段：从 (x_i, y_i, 0) 到 (x_i, y_i, z_current)
      drawnow; pause(1/(n_pillar*5))
      if recorder, writeVideo(recorder, getframe(gcf)); end
  end
  pause(dt)  % 完成生长后停顿
  ```
- 颜色：立柱 `#0072BD`（`[0 114 189]/255`），`LineWidth=2`

**阶段 2 · 橙母线铺出**
- `title('阶段2：直线构成曲面')`
- 先画半透明曲面片：`surf(X,Y,Z,'FaceAlpha',alpha_surf,'EdgeColor','none','FaceColor',#EDB120)`
- 然后铺 u=const 组（`#EDB120`），再铺 v=const 组（`#FFD060`）
- 每组内沿参数扫掠（u 从小到大、v 从小到大），每条执行**延展动画**：
  ```
  for frame = 1:n_rule          % 默认 10 帧
      t = frame / n_rule
      更新 plot3 线段：从 P_neg 到 lerp(P_neg, P_pos, t)
      drawnow; pause(1/(n_rule*5))
      if recorder, writeVideo(recorder, getframe(gcf)); end
  end
  pause(dt)
  ```
- 母线 `LineWidth=1.5`

**终态**: `pause(1.5)` 停留 1.5 秒供观察。若录制模式，追加 `round(1.5*fps)` 帧终态画面。

> **生长/延展实现方式**：不创建新 plot3 对象，而是在循环外创建句柄，循环内通过 `set(h, 'XData', ..., 'YData', ..., 'ZData', ...)` 更新线段端点，避免句柄泄露。此方式也保证 VideoWriter 每帧正确捕获。

### 6.3 `run_nest_demo.m`

驱动入口：

```matlab
function run_nest_demo(opts)
% opts.record (false|true), opts.out ('nest.mp4'), opts.gif (false|true)
  p = default_params();              % §5 默认值
  G = nest_geometry(p);
  fig = figure('Color','w','Position',[100 100 900 700]);
  ax  = axes(fig);
  if opts.record
    vw = VideoWriter(opts.out, 'MPEG-4'); vw.FrameRate = p.fps; open(vw);
    nest_animation(G, p, ax, vw);
    close(vw);
    if opts.gif, nest_to_gif(opts.out, [opts.out '.gif']); end
  else
    nest_animation(G, p, ax, []);
  end
end
```

### 6.4 `test_nest_geometry.m`

单元测试，验证几何正确性：

1. **母线中点在曲面上**：对每条母线两端点 `P₁, P₂`，取中点 `M = (P₁+P₂)/2`，验证 `M.z ≈ c·(M.x²/a² − M.y²/b²)`，容差 `1e-10`。→ 证明线段在曲面上是直线。
2. **立柱在椭圆上**：每根立柱 `(x_i, y_i)` 满足 `(x_i/a)²+(y_i/b)² = 1`。
3. **柱顶贴合曲面**：`z_i ≈ c·(x_i²/a² − y_i²/b²)`。
4. **u=const 母线为直线**：除中点外，任取 `t∈[0,1]` 的插值点也在曲面上。

## 7. 视觉规范

- **配色**：立柱 `#0072BD`（RGB `[0 114 189]/255`），u 组母线 `#EDB120`（`[237 177 32]/255`），v 组母线 `#FFD060`（`[255 208 96]/255`），曲面片 `FaceColor #EDB120` alpha 0.2，地面环 `#7F8C8D`，坐标轴 `k`（黑）。
- **线宽**：立柱 `LineWidth=2`，母线 `LineWidth=1.5`。
- **背景**：白色 `figure('Color','w')`，投影仪友好。
- **标注**：三轴标签 `xlabel('x')` / `ylabel('y')` / `zlabel('z')`；阶段标题随动画切换（『阶段1：立柱升起』『阶段2：直线构成曲面』）。
- **地面**：仅椭圆环（`#7F8C8D`），不画方格网。

## 8. 动画时序（总时长估算）

| 阶段 | 内容 | 时长 |
|------|------|------|
| 0 | 空坐标系展示 | `hold_t` = 0.8 s |
| 1 | 10 根蓝立柱逐根升起（每根生长 8 帧 + 间隔 dt） | `N × (8/150 + dt)` ≈ 10×0.2 = 2.0 s |
| - | 阶段间停顿 | `hold_t` = 0.8 s |
| 2 | 曲面片淡入 | 0.3 s |
| 2 | u 组 8 条 + v 组 8 条母线条条延展（每条约 10 帧 + dt） | `16 × (10/150 + 0.15)` ≈ 16×0.22 ≈ 3.5 s |
| - | 终态停留 | 1.5 s |
| **合计** | | **≈ 8.9 s** |

> 录制帧数 ≈ `8.9 × 30 ≈ 267 帧`（主动帧循环录制保证节奏准确，不依赖 pause 实际耗时）。gif 导出也从同一帧循环中采集，保持一致性。

## 9. 错误处理与边界

- 参数非法（`N<3`, `a<=0`, `c<0`）：`nest_geometry` 抛 `error('NEST:param', ...)`，附中文提示。
- `VideoWriter` 不可用（无编解码器）：捕获警告，降级为只跑实时动画，提示用户。
- 母线条数过少（`M<2`）：自动夹到 `M=2`。
- 域 `u,v∈[−1,1]` 严格闭区间，避免浮点抖动导致母线端点偏出曲面（中点测试容差覆盖）。

## 10. Grill 决议摘要

以下开放问题已通过 grill-with-docs 全部解决，决策已融入规范各章节：

| 原问题 | 决议 | 融入位置 |
|--------|------|----------|
| 母线颜色统一 vs 区分 | u=#EDB120, v=#FFD060 深浅区分 | §3, §7 |
| VideoWriter 录制策略 | 主动帧循环（每间帧 writeVideo） | §3, §6.2 |
| 椭圆比例（1.5→1.2） | a=6, b=5，贴近真实鸟巢（比例 1.2） | §3, §5 |
| 「逐根出现」视觉形态 | 生长/延展动画（立柱逐帧升起，母线逐帧延展） | §3, §6.2 |
| 立柱出现顺序 | 逆时针逐根（θ=0 起） | §3, §6.2 |
| 母线出现顺序 | 先 u 组，后 v 组 | §3, §6.2 |
| 曲面片出现时机 | 阶段 2 开始先曲面再母线 | §3, §6.2 |
| 母线延展方向 | 同向延展（负→正） | §3, §6.2 |
| 母线内部顺序 | 沿参数扫掠（u/v 从小到大） | §3, §6.2 |
| 相机动画 | 全程固定 az=−35°, el=30° | §3 |
| gif 导出 | 需要（mp4 + gif 双格式） | §3, §6.3 |
| 生长/延展帧数 | 立柱 ~8帧/根，母线 ~10帧/条 | §5, §6.2 |

**已验证（prototype 通过）**：

| # | 问题 | 结果 |
|---|------|------|
| P1 | `surf` 半透明片在 `VideoWriter` 录制下 alpha 是否正确 | ✅ 通过。MPEG-4 编码器正确捕获半透明曲面片 |
| P2 | 生长/延展动画用 `set(h,'ZData',...)` 逐帧更新 + `writeVideo` 是否流畅 | ✅ 通过。`set`+`drawnow`+`writeVideo` 三者配合无报错，零长初态→逐帧生长亦可 |
| P3 | gif 从帧循环采集的颜色保真度（256 色调色板 vs 橙蓝色） | ✅ 通过。`rgb2ind(...,256,'nodither')` 可生成 gif，橙色可辨 |

**原型附注**：
- 正式代码中必须用**零长初态**创建句柄（`plot3` 起点=终点），再逐帧 `set` 更新，避免「先全出现再生长」问题。
- gif 单帧采集已成功；动画 gif 需在帧循环中逐帧追加 `imwrite(...,'WriteMode','append')`。

## 11. 验收标准

- [ ] `test_nest_geometry.m` 全部通过（母线中点容差 1e-10）。
- [ ] `run_nest_demo` 实时运行显示三阶段，侧俯视角正确。
- [ ] `run_nest_demo(record=true)` 产出 `nest.mp4`，可在系统播放器播放。
- [ ] 蓝立柱与橙母线颜色分明，肉眼可辨。
- [ ] 母线肉眼可见为直线，交织勾勒出马鞍面。
- [ ] 课堂讲解者可在每阶段停顿处口述要点。

## 12. 文件清单（交付物）

- `src/matlab/nest_geometry.m` — 纯几何计算
- `src/matlab/nest_animation.m` — 三阶段生长/延展动画
- `src/matlab/run_nest_demo.m` — 驱动入口 + record/gif 开关
- `src/matlab/test/test_nest_geometry.m` — 单元测试套件
- `out/nest.mp4` — 导出视频（主动帧循环录制）
- `out/nest.gif` — 导出动画（适用于嵌入 PPT）
- `README.md` — 运行说明

## 附：术语表（Ubiquitous Language）

| 术语 | 定义 |
|------|------|
| 双曲抛物面 (Hyperbolic Paraboloid) | `z = x²/a² − y²/b²`，马鞍形二次曲面 |
| 双重直纹面 (Doubly Ruled Surface) | 过每点恰有两条直线落在曲面上的曲面 |
| 母线 (Ruling / Generator) | 落在曲面上的直线 |
| u=const 母线 | 固定 u 参数得到的母线（一组） |
| v=const 母线 | 固定 v 参数得到的母线（另一组） |
| 立柱 (Pillar) | 椭圆环上垂直地面的支撑杆 |
| 阶段 (Stage) | 动画的一个语义单元（0/1/2） |
| 过渡 (Transition) | 阶段内/间构件逐根出现的动画 |
